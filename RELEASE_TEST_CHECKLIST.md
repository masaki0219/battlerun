# RELEASE_TEST_CHECKLIST

TestFlight ビルドでの通し検証用チェックリスト。サーバー集計・スケジューラ・Webhook・
プッシュ通知はいずれも Expo Go では検証できないため、**TestFlight ビルドが必須**。

各項目に沿って確認し、チェックボックスを埋めながら進めること。

## デプロイ順序（必須）

1. Functions（`submitActivity` / `aggregateActivity` / badges）
2. Firestore indexes
3. Firestore / Storage rules
4. Hosting（利用規約・プライバシーポリシー）
5. 上記の動作確認後にアプリを配布

新しいアプリは `submitActivity` Callableを使うため、Functionsより先に配布しないこと。

## 追加された回帰テスト

- [ ] 他ユーザーの活動詳細でGPSルートが表示・取得されない
- [ ] 本人の活動詳細ではGPSルートが表示される
- [ ] 記録停止時に通信を切り、再起動・再接続後に1回だけ保存・集計される
- [ ] Functionsを同じイベントで再試行しても距離が二重加算されない
- [ ] 全陣営0kmでは順位が表示されず、同距離では同順位になる
- [ ] ラン直後にホームの週間距離、自分の距離、陣営内順位が更新される
- [ ] 開催中チャレンジでは最終結果画面を表示できない
- [ ] 終了通知タップで結果画面、終了前通知タップで詳細画面が開く
- [ ] アカウント削除後に公開プロフィール、GPSルートチャンク、Storage画像が残らない

## 前提条件

- [ ] TestFlight ビルドを配布済み（EAS Build → TestFlight アップロード）
- [ ] テスト用アカウントを2つ以上用意（自分用 + 他者役）
- [ ] 開催中の公開バトルが最低1つ存在する（[OPERATIONS.md](./OPERATIONS.md) 参照）
- [ ] Firebase コンソール・RevenueCat ダッシュボードへのアクセス権がある
- [ ] RevenueCat サンドボックス課金用の Apple ID（Sandbox Tester）を用意済み

---

## シナリオ1: 新規ユーザーの Day-0

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 1-1 | 新規アカウントで登録 | サインアップ完了、ホームに未参加ユーザー向けレイアウトが表示される | アプリ（バトルタブ） |
| 1-2 | バトルタブを開く | 「開催中の作戦に参加しよう」カードが表示される。人数最少陣営に「援軍募集中！」バッジが出る | アプリ（バトルタブ） |
| 1-3 | 陣営を選んで参加 | 参加処理がエラーなく完了し、参加中バトルとして表示が切り替わる | アプリ |
| 1-4 | `battles/{id}/participants/{uid}` を確認 | `userId`・`categoryId`・`totalDistanceKm: 0` が作成されている | Firebaseコンソール（Firestore） |
| 1-5 | `battles/{id}/category_stats/{categoryId}` を確認 | `participantCount` が参加前より+1されている（クライアントからの直接更新ではなく `participantCounter` 経由） | Firebaseコンソール（Firestore） |
| 1-6 | GPSモードで初回ランを記録・終了 | summary画面に遷移し、順位変動（変化なしなら「初回記録」等の表示）が出る | アプリ（record/summary） |
| 1-7 | 数秒〜数十秒待って activity を確認 | `activities/{id}.aggregated === true` になる。`participants.totalDistanceKm` と `category_stats.totalDistanceKm` に加算されている | Firebaseコンソール（Firestore） |

**Expo Goで検証可能か**: 1-1〜1-3, 1-6 は可（GPSはシミュレータ座標で代用可）。1-4, 1-5, 1-7 のサーバー集計反映はFirestoreコンソールで確認するため理論上Expo Goでも見えるが、**push/スケジューラ系との整合確認を含めTestFlight推奨**。

---

## シナリオ2: 帰属ループ（リアクション → プッシュ）

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 2-1 | アカウントAの活動記録に、アカウントBからリアクションを送る | `activities/{id}/reactions/{bUid}` が作成される | Firebaseコンソール |
| 2-2 | アカウントAの端末でプッシュ通知を受信 | 数秒以内にプッシュ通知（リアクション内容）が届く | アプリA（実機・バックグラウンド状態で確認） |
| 2-3 | 通知をタップ | 該当の `activity/[id]` 画面が直接開く（ホームに落ちない） | アプリA |
| 2-4 | `users/{aUid}/notifications` を確認 | 通知ドキュメントが作成され、既読化されている（開いた後） | Firebaseコンソール |

**Expo Goで検証可能か**: TestFlight必須（プッシュ実配信はExpo Goでは検証不可）。

---

## シナリオ3: 競争ループ（順位変動プッシュ）

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 3-1 | 自陣営が僅差で負けている状態から、順位が入れ替わるだけの距離を記録 | 記録は正常に集計される | アプリ / Firebaseコンソール |
| 3-2 | `rankChangeScheduler`（60分毎）の実行を待つ、または手動トリガーで確認 | 順位変動を検知したユーザーにプッシュが届く | 実機 / Firebase Functionsログ |
| 3-3 | 同日中にさらに2回、別の順位変動を発生させる（3回目） | 3回目まではプッシュが届く | 実機 |
| 3-4 | 同日中に4回目の順位変動を発生させる | 4回目はプッシュが届かない（1日3回上限） | 実機 / Functionsログで送信スキップを確認 |
| 3-5 | 日付が変わった後に再度順位変動を発生させる | 上限がリセットされ、プッシュが届く | 実機 |

**Expo Goで検証可能か**: TestFlight必須。スケジューラの実行間隔（60分）を待てない場合は Firebase コンソールから該当関数を手動実行して確認する。

---

## シナリオ4: バトル終了（称号・シェア）

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 4-1 | `endAt` を過去に設定したテスト用バトルを用意（または本番バトルの終了を待つ） | - | Firebaseコンソール / admin画面 |
| 4-2 | `battleStatusScheduler`（60分毎）の実行を待つ | バトルの `status` が `finished` に変わる | Firebaseコンソール |
| 4-3 | 参加者全員の端末を確認 | バトル終了プッシュが届く | 実機 |
| 4-4 | 優勝/準優勝陣営の全メンバーの `users/{uid}.titles` を確認 | 称号が付与されている。**結果画面を開いていないアカウントにも付与されていること** | Firebaseコンソール |
| 4-5 | `titlesAwardedAt` を確認 | 一度だけ設定され、再実行しても重複付与されない（冪等性） | Firebaseコンソール |
| 4-6 | 結果画面を開く | 順位・称号が表示される | アプリ |
| 4-7 | Freeアカウントで画像シェア | 画像に「ZELIO」の透かしが入る | アプリ（結果画面のシェアボタン） |
| 4-8 | Proアカウントで画像シェア | 透かしなしで画像が生成される | アプリ |
| 4-9 | シェアに失敗する状況を作る（例: 共有シート非対応環境） | テキストシェアに自動フォールバックする | アプリ |

**Expo Goで検証可能か**: TestFlight必須（スケジューラ・プッシュ・称号付与はサーバー処理のため）。

---

## シナリオ5: 課金（RevenueCat）

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 5-1 | Sandbox Apple IDでPro購入を実行 | 購入完了後、即座にPro表示に切り替わる（entitlement反映） | アプリ |
| 5-2 | 数秒〜数十秒待つ | RevenueCat Webhook経由で `users/{uid}.plan === 'pro'` になる | Firebaseコンソール |
| 5-3 | アプリを再インストールし「購入の復元」を実行 | Pro状態が復元される | アプリ |
| 5-4 | RevenueCatダッシュボードでサブスクリプションを即時失効させる（テスト用） | Webhookの `EXPIRATION` イベント受信後、`plan` が `free` に戻る | Firebaseコンソール / RevenueCatダッシュボード（Webhookログ） |
| 5-5 | Pro購入ボタン周辺の表示を確認 | 価格・期間・自動更新の説明文、「購入の復元」導線がある | アプリ（プロフィール画面） |

**Expo Goで検証可能か**: TestFlight必須（RevenueCat実課金・Webhookのため）。

---

## シナリオ6: GPS異常系

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 6-1 | 記録中に機内モードON等でGPSを途絶させる | 約20秒後に警告バナーが表示される（不安定/フォアグラウンドのみ/権限なしのいずれか適切な文言） | アプリ（record画面） |
| 6-2 | GPSを復帰させる | 警告バナーが自動的に消える | アプリ |
| 6-3 | 記録中にアプリをバックグラウンドにする | バックグラウンドでも距離の加算が継続する（bg/fg二重加算が発生しない） | アプリ・記録結果 |
| 6-4 | バックグラウンド→フォアグラウンド復帰を数回繰り返す | 距離が二重加算されず正しい値になる | アプリ・記録結果 |
| 6-5 | 位置情報の権限を「許可しない」にして記録開始 | 権限なしの警告バナーが表示され、記録が破綻しない | アプリ |

**Expo Goで検証可能か**: フォアグラウンドの基本動作はExpo Goでも一部確認可能。バックグラウンド計測・権限まわりの実機挙動は**TestFlight推奨**（iOSのバックグラウンド位置情報制御はExpo Goと実ビルドで挙動が異なるため）。

---

## シナリオ7: アカウント削除

| # | 手順 | 期待結果 | 確認場所 |
|---|---|---|---|
| 7-1 | 参加中バトルがある状態でアカウント削除を実行（パスワード確認あり） | 削除完了後ログイン画面に遷移する | アプリ |
| 7-2 | 削除前の `participants` 所属陣営の `category_stats.participantCount` を確認 | 削除後、自動的に-1補正されている（`participantCounter` 経由） | Firebaseコンソール |
| 7-3 | 削除したuidの `activities` を検索 | 0件になっている（GPSルートを含め完全に削除） | Firebaseコンソール |
| 7-4 | 削除したuidの `battles/*/participants/{uid}` を検索（collectionGroup） | 0件になっている | Firebaseコンソール |
| 7-5 | 削除したuidの `users/{uid}/notifications`、`users/{uid}/badges` を確認 | サブコレクションが空になっている | Firebaseコンソール |
| 7-6 | 削除したuidの `users/{uid}` 本体を確認 | ドキュメントが存在しない | Firebaseコンソール |
| 7-7 | アバター画像をアップロード済みの状態で削除 | Storageの `avatars/{uid}` が削除されている | Firebaseコンソール（Storage） |

**注意**: `onUserDeleted` の `collectionGroup('participants')` 検索は、参加ドキュメントの `userId` フィールドに依存する。本リリース準備で導入したフィールドのため、**このリリース以降に参加したデータのみ対象**（導入前の参加データは対象外）。

**Expo Goで検証可能か**: TestFlight推奨（Cloud Functionsのトリガー実行を確認するため）。

---

## 総括表

| シナリオ | Expo Goで検証可能か | TestFlight必須か |
|---|---|---|
| 1. Day-0 | 一部可 | 推奨 |
| 2. 帰属ループ | 不可 | 必須 |
| 3. 競争ループ | 不可 | 必須 |
| 4. バトル終了 | 不可 | 必須 |
| 5. 課金 | 不可 | 必須 |
| 6. GPS異常系 | 一部可（フォアグラウンドのみ） | 推奨（バックグラウンドは必須） |
| 7. アカウント削除 | 一部可（Firestore確認のみ） | 推奨 |
